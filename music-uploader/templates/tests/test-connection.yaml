apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "music-uploader.fullname" . }}-test-connection"
  labels:
    {{- include "music-uploader.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "music-uploader.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
